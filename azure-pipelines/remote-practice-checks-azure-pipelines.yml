trigger:
  branches:
    include:
    - main
    - develop
  paths:
    include:
    - tests/remote_practice_checks/**
    - tests/shared/**

pool:
  vmImage: 'ubuntu-latest'

variables:
  pythonVersion: '3.11'

jobs:
- job: RemotePracticeChecksValidation
  displayName: 'Remote Practice Checks API Validation'
  
  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '$(pythonVersion)'
    displayName: 'Use Python $(pythonVersion)'

  - script: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt
    displayName: 'Install dependencies'

  - script: |
      pytest tests/remote_practice_checks/ -v --junitxml=test-results.xml
    displayName: 'Run Remote Practice Checks tests'
    env:
      API_BASE_URL: $(QA_API_BASE_URL)
      API_TOKEN: $(QA_API_TOKEN)

  - task: PublishTestResults@2
    condition: succeededOrFailed()
    inputs:
      testResultsFiles: 'test-results.xml'
      testRunTitle: 'Remote Practice Checks API Tests'

  - script: |
      if [ $? -ne 0 ]; then
        echo "##vso[task.logissue type=error]Remote Practice Checks API validation failed - blocking deployment"
        exit 1
      fi
    displayName: 'Validate test results'
