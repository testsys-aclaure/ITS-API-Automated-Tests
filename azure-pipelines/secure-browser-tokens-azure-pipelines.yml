trigger:
  branches:
    include:
    - main
    - develop
  paths:
    include:
    - tests/secure_browser_tokens/**
    - tests/shared/**

pool:
  vmImage: 'ubuntu-latest'

variables:
  pythonVersion: '3.11'

jobs:
- job: SecureBrowserTokensValidation
  displayName: 'Secure Browser Tokens API Validation'
  
  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '$(pythonVersion)'
    displayName: 'Use Python $(pythonVersion)'

  - script: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt
    displayName: 'Install dependencies'

  - script: |
      pytest tests/secure_browser_tokens/ -v --junitxml=test-results.xml
    displayName: 'Run Secure Browser Tokens tests'
    env:
      API_BASE_URL: $(QA_API_BASE_URL)
      API_TOKEN: $(QA_API_TOKEN)

  - task: PublishTestResults@2
    condition: succeededOrFailed()
    inputs:
      testResultsFiles: 'test-results.xml'
      testRunTitle: 'Secure Browser Tokens API Tests'

  - script: |
      if [ $? -ne 0 ]; then
        echo "##vso[task.logissue type=error]Secure Browser Tokens API validation failed - blocking deployment"
        exit 1
      fi
    displayName: 'Validate test results'
